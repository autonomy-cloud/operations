{{- if and .Values.postgresql.enabled (not .Values.postgresOperator.cnpg.enabled) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-postgresql-primary-config
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}-postgresql
    app.kubernetes.io/part-of: cast-operations
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: postgresql
    appname: cast-operations
data:
  postgresql.conf: |
{{ .Values.postgresql.primary.configuration | indent 4 }}
{{- end }}