# CLI Cast Operations

CLI Cast Operations — это интерфейс командной строки для управления ресурсами Cast Operations непосредственно из терминала. Поддерживает полные CRUD-операции с мониторами, инцидентами, алертами, страницами статуса и многим другим.

## Возможности

- **Поддержка нескольких сред** с именованными контекстами для production, staging и development
- **Автоматическое обнаружение** доступных ресурсов из вашего экземпляра Cast Operations
- **Гибкая аутентификация** через флаги CLI, переменные окружения или сохранённые контексты
- **Умное форматирование вывода** с режимами отображения JSON, таблицы и расширенного формата
- **Поддержка скриптов** для конвейеров CI/CD и рабочих процессов автоматизации

## Установка

```bash
npm install -g @oneuptime/cli
```

## Быстрый старт

```bash
# Аутентификация в вашем экземпляре Cast Operations
oneuptime login <your-api-key> https://visca.ai

# Список ваших мониторов
oneuptime monitor list

# Просмотр конкретного инцидента
oneuptime incident get <incident-id>

# Просмотр всех доступных ресурсов
oneuptime resources
```

## Документация

| Руководство                                      | Описание                                                         |
| ------------------------------------------------ | ---------------------------------------------------------------- |
| [Аутентификация](./authentication.md)            | Вход, контексты и управление учётными данными                    |
| [Операции с ресурсами](./resource-operations.md) | CRUD-операции с мониторами, инцидентами, алертами и другим       |
| [Форматы вывода](./output-formats.md)            | Режимы вывода JSON, таблицы и расширенного формата               |
| [Скриптинг и CI/CD](./scripting.md)              | Автоматизация, переменные окружения и использование в конвейерах |
| [Справочник команд](./command-reference.md)      | Полный справочник по всем командам и опциям                      |

## Глобальные опции

Эти флаги можно использовать с любой командой:

| Флаг                    | Описание                                       |
| ----------------------- | ---------------------------------------------- |
| `--api-key <key>`       | Переопределить ключ API для этой команды       |
| `--url <url>`           | Переопределить URL экземпляра для этой команды |
| `--context <name>`      | Использовать конкретный именованный контекст   |
| `-o, --output <format>` | Формат вывода: `json`, `table`, `wide`         |
| `--no-color`            | Отключить цветной вывод                        |
| `--help`                | Показать справку по команде                    |
| `--version`             | Показать версию CLI                            |

## Получение справки

```bash
# Общая справка
oneuptime --help

# Справка по конкретной команде
oneuptime monitor --help
oneuptime monitor list --help
```
