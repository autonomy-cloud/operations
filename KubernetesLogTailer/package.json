{
  "name": "@cast-operations/kubernetes-log-tailer",
  "version": "11.5.14",
  "description": "Cast Operations Kubernetes log tailer — collects pod logs via the Kubernetes API (GKE Autopilot compatible) and forwards to Cast Operations via OTLP-HTTP.",
  "repository": {
    "type": "git",
    "url": "https://github.com/autonomy-cloud/operations"
  },
  "main": "Index.ts",
  "scripts": {
    "start": "node --require ts-node/register Index.ts",
    "compile": "tsc",
    "dev": "npx nodemon",
    "test": "npm run compile && node --test Tests/NamespaceFilter.test.js"
  },
  "author": "Cast Operations <hello@latticeruntime.com> (https://latticeruntime.com/)",
  "license": "Apache-2.0",
  "dependencies": {
    "@kubernetes/client-node": "^1.4.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "nodemon": "^3.0.0"
  }
}
